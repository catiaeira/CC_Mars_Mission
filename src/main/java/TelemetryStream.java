public class TelemetryStream { // TCP protocol
    /*
    - establish a trusted connection between rover and mothership
    - send telemetry data (rover id, position, state)
    - talk with multiple rovers at the same time
     */
}
